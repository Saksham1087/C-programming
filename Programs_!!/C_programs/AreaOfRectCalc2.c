#include <stdio.h>
int main() {
    int a= 15;  // length of a rectangle 
    int b= 30;  // breadth of a rectangle 
    int c=a*b;  // area of a rectangle
    
    printf("length of a rectangle is %d unit\n",a);
    printf("breadth of a rectangle is %d unit\n\n",b);
    printf("The area of rectangle is %d sq.unit",c);
    return 0;
}